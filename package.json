{
  "name": "rsvp-infra",
  "version": "1.0.0",
  "description": "SLS API gateway and DB infrastructure",
  "main": "handler.js",
  "license": "MIT",
  "scripts": {
    "test": "yarn jest ./"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.6.0",
    "aws-sdk": "^2.849.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "aws-sdk-mock": "^5.1.0",
    "jest": "^26.6.3"
  }
}
